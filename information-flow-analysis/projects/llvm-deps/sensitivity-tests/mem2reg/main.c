#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define PRINT printf(" ")

int function(int key) {
  int i, b = rand();
  srand(time(NULL));
  i = key;
  if (i)
    ;

  i = b + rand();
  if (i)
    ;

  return i;
}
